{
  "name": "discord-bot",
  "type": "module",
  "version": "1.0.0",
  "description": "A discord bot for moderation.",
  "main": "app.js",
  "scripts": {
    "start": "node -r dotenv/config app.js",
    "dev": "nodemon -r dotenv/config app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christopher Scott",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "js-yaml": "^4.0.0",
    "node-localstorage": "^2.1.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
